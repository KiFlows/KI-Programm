# Prompt — Skills hinzufügen

> An den **Skill-Manager-Agent** (Agent_00) geben. Hänge die ausgefüllten Vorlagen an (mit +), dann diesen Prompt einfügen.

## Prompt
- [ ] Erledigt

>DE
```kopieren
Die Vorlagen sind angehängt. Lege je Datei einen Skill an — eines nach dem anderen.
Pro Datei: die drei Werte oben (Interner Name / Sichtbarer Name / Beschreibung) genau übernehmen, alles unter der Linie = Skill-Inhalt.
```

> [!code]- English version (Klicke zum Ausklappen)
> ```kopieren
> The templates are attached. Create one skill per file — one after another.
> Per file: take the three values at the top (Internal name / Visible name / Description) exactly, everything below the line = skill content.
> ```
