The templates are attached. Create one skill per file — one after another.
Per file: take the three values at the top (**Internal name** / **Visible name** / **Description**) exactly; everything below the line = skill content.
